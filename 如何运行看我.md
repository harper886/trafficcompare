# 如何运行本项目

## 1. 正确下载项目

本项目的数据集和模型权重包含较大的 `.npy`、`.h5` 文件，已经使用 Git LFS 管理。

不要直接使用 GitHub 页面里的 `Download ZIP`，否则可能只下载到 LFS 指针文件，运行时会提示数据集损坏。

推荐使用下面的方式下载：

```powershell
git lfs install
git clone https://github.com/harper886/trafficcompare.git
cd trafficcompare
git lfs pull
```

如果已经 clone 过项目，只需要在项目目录执行：

```powershell
git lfs install
git lfs pull
```

可以用下面命令检查大文件是否完整：

```powershell
Get-Item nyc\data_nyc.npy,chicago\data_chicago.npy
```

正常情况下：

- `nyc\data_nyc.npy` 约 390 MB
- `chicago\data_chicago.npy` 约 110 MB

如果文件只有几百字节，说明 Git LFS 数据没有拉取成功。

## 2. 启动前端页面

必须通过本地 Web 服务打开页面，不能直接双击 HTML 文件。否则浏览器可能无法读取 `results/` 目录下的 JSON 数据。

在项目根目录运行：

```powershell
python -m http.server 8000
```

然后在浏览器访问：

```text
http://localhost:8000/dashboard_fixed.html
```

## 3. 前端数据说明

页面默认会优先加载：

- `results/frontend_predictions_nyc.json`
- `results/frontend_predictions_chicago.json`
- `results/frontend_metrics.json`
- `results/frontend_topology.json`

当前前端展示的是从本地数据和模型结果导出的代表性样本：

- NYC：13 个时间帧，64 个区域，832 条区域-时间记录
- Chicago：13 个时间帧，27 个区域，351 条区域-时间记录

如果页面提示没有读取到本地 JSON 数据，说明当前使用的是内置演示兜底数据。请检查是否通过 `python -m http.server` 启动，以及 `results/` 目录是否存在。

## 4. 答辩表述建议

可以表述为：

> 本系统使用 NYC 和 Chicago 交通时序数据、事故标签、空间邻接关系和模型评估结果完成训练与分析。前端为了保证交互性能，没有直接加载全量 `.npy` 文件，而是通过导出脚本抽取代表性时间帧，并生成前端可读的 JSON 文件进行可视化展示。

不要表述为：

- 实时线上交通系统
- 已经接入完整真实地图坐标和实时事故接口
- 前端所有文字和区域描述都来自真实数据库
