# 答辩 PPT 大纲与每页讲稿

建议总页数：12 页左右。答辩时间如果是 8 分钟，每页平均 35 到 45 秒；如果是 10 分钟，可以适当展开实验和系统展示。

## 第 1 页 封面

**标题**

基于 Attention-LSTM 的交通碰撞风险预测与可解释分析系统

**页面内容**

- 论文题目
- 学生姓名、专业、指导老师
- 学校和答辩日期
- 可放图片：`assets/promo-defense-cover.png` 或 `assets/promo-thesis-cover.png`

**讲稿**

各位老师好，我的毕业设计题目是《基于 Attention-LSTM 的交通碰撞风险预测与可解释分析系统》。本项目围绕城市交通碰撞风险预警问题，使用 NYC 和 Chicago 双城交通数据进行建模实验，并实现了一个前端可视化大屏，用于展示风险预测、真实标签校验和可解释分析结果。

## 第 2 页 研究背景与问题

**页面内容**

- 城市交通事故具有突发性和时空相关性
- 传统统计方法难以捕捉复杂时序变化
- 交通安全场景中漏报代价较高
- 研究目标：提前识别高风险区域和时间片

**推荐图**

- `assets/project-intro.png`

**讲稿**

城市交通碰撞通常受到道路结构、历史流量、区域功能和时间变化等多种因素影响，具有明显的时空相关性。对于交通预警系统来说，最关键的问题不是简单判断整体是否准确，而是尽量提前发现潜在高风险区域，减少漏报。因此，本项目的目标是构建一个能够结合时序特征和空间关系的风险预测模型，并通过可视化系统对结果进行展示和解释。

## 第 3 页 数据集构建

**页面内容**

- 数据集：NYC、Chicago
- 输入：交通时序数据、事故标签、区域坐标、空间邻接矩阵
- 输出：`data_*.npy`、`label.npy`、`dict_xy.npy`、邻接矩阵

**推荐图**

- `assets/paper-dataset-construction.png`
- 或 Mermaid：`论文图表汇总.md` 图 2

**关键表格**

| 数据集 | 时间片 | 区域数 | 特征维度 | 样本数 | 正样本比例 |
| --- | ---: | ---: | ---: | ---: | ---: |
| NYC | 13,128 | 64 | 116 | 840,192 | 22.53% |
| Chicago | 8,784 | 27 | 116 | 237,168 | 13.38% |

**讲稿**

本项目使用了 NYC 和 Chicago 两个城市数据集。每个样本由时间片、区域和特征维度组成，标签表示该区域在对应时间片是否存在碰撞或异常风险。除了交通时序数据外，项目还使用了区域坐标映射和道路、POI、历史记录等空间邻接关系，为后续模型提供时序和空间两个维度的信息。

## 第 4 页 技术路线

**页面内容**

- 数据构建
- 模型训练
- 实验评估
- 前端导出
- 可视化展示

**推荐图**

- `论文图表汇总.md` 图 10
- 或 `项目流程图.md` 总体流程

**讲稿**

整体技术路线分为五个部分。首先对交通时序数据、事故标签和空间邻接关系进行整理；然后构造模型输入窗口，训练 Attention-LSTM 风险预测模型；接着与多种基线模型进行对比，并通过消融实验验证关键模块的有效性；最后将预测结果、实验指标和拓扑关系导出为前端 JSON，在大屏中进行可视化展示。

## 第 5 页 模型结构

**页面内容**

- 输入序列：历史交通特征
- Attention 模块：突出关键时间片和区域特征
- LSTM 编码器：建模时序依赖
- 输出：风险概率和预警结果

**推荐图**

- `assets/paper-model-structure.png`
- 或 Mermaid：`论文图表汇总.md` 图 4

**讲稿**

模型部分采用 Attention-LSTM 结构。LSTM 用于捕捉交通数据中的时间依赖关系，Attention 模块用于对不同时间片和特征赋予不同权重，使模型更加关注关键波动。最终模型输出每个区域在目标时间片上的风险概率，再经过阈值判定和平滑后处理得到预警结果。

## 第 6 页 模型优化与后处理

**页面内容**

- 平滑门：减少预测概率剧烈抖动
- 流式后处理：提升连续时间片输出稳定性
- 目标：在高召回的同时保持结果稳定

**推荐图**

- `assets/paper-ablation-study.png`

**讲稿**

由于交通碰撞风险具有连续性，如果预测结果在相邻时间片之间频繁跳变，会影响实际预警使用。因此项目引入了平滑门和流式后处理机制，用于稳定风险概率变化。这样可以在保证召回能力的同时，减少前端展示和实际预警中的不稳定输出。

## 第 7 页 实验设计

**页面内容**

- 对比模型：GSNet、STG2Seq、ConvLSTM、LSTM、LightGBM、XGBoost、ARIMA、HA 等
- 评价指标：AUC-PR、AUC-ROC、F1、Accuracy、Recall
- 重点关注：AUC-PR、F1、Recall

**推荐图**

- `assets/paper-experiment-comparison.png`
- 或 Mermaid：`论文图表汇总.md` 图 5

**讲稿**

实验部分将本文模型与多种深度学习模型、传统机器学习模型和统计方法进行对比。评价指标包括 AUC-PR、AUC-ROC、F1、Accuracy 和 Recall。由于交通碰撞样本存在类别不平衡，单纯 Accuracy 不能充分反映模型能力，因此本文更关注 AUC-PR、F1 和 Recall，尤其是召回率。

## 第 8 页 实验结果

**页面内容**

| 数据集 | 模型 | AUC-PR | AUC-ROC | F1 | Recall |
| --- | --- | ---: | ---: | ---: | ---: |
| NYC | Myplan | 0.6955 | 0.8786 | 0.6443 | 0.8265 |
| Chicago | Myplan | 0.5617 | 0.8290 | 0.4730 | 0.7055 |

- NYC 上 AUC-PR、AUC-ROC、F1、Recall 表现较优
- Chicago 上 Myplan 在 AUC-PR、AUC-ROC、F1、Recall 上领先

**讲稿**

从实验结果看，本文模型在 NYC 和 Chicago 两个数据集上都取得了较好的综合表现。尤其是在 AUC-PR 和 Recall 上，模型能够更有效地识别高风险样本。对于交通安全场景来说，召回率的提升意味着模型可以发现更多潜在风险区域，从而减少漏报。

## 第 9 页 消融实验

**页面内容**

| 配置 | NYC Recall | Chicago Recall |
| --- | ---: | ---: |
| 去平滑门 + 去流式后处理 | 0.7108 | 0.6318 |
| 去平滑门 | 0.8046 | 0.6693 |
| 去流式后处理 | 0.7407 | 0.6401 |
| 完整模型 | 0.8265 | 0.7055 |

**推荐图**

- `assets/paper-ablation-study.png`

**讲稿**

消融实验用于验证平滑门和流式后处理的有效性。可以看到，完整模型在两个城市数据集上的 Recall 均高于去除模块后的版本，说明这两个模块对提升风险识别能力和稳定模型输出都有帮助。这也证明模型提升不是单纯来自基础 LSTM，而是多个模块协同作用的结果。

## 第 10 页 前端可视化系统

**页面内容**

- 风险热力地图
- 时间轴播放
- TP / FP / FN 标签校验
- 模型指标对比图
- 空间邻接解释面板

**推荐图**

- `assets/frontend-real-screenshot.png`
- `assets/promo-dashboard-showcase.png`

**讲稿**

为了让模型结果更直观，本项目实现了一个前端可视化大屏。页面中可以切换 NYC 和 Chicago 城市，查看不同时间帧下的风险热力图，并展示 TP、FP、FN 等真实标签校验结果。同时，右侧面板会展示当前区域的流量特征、风险概率和空间邻接关系，使模型结果更容易解释。

## 第 11 页 前端数据说明与系统边界

**页面内容**

- 前端默认读取导出的 JSON 文件
- NYC：13 帧、64 区域、832 条区域-时间记录
- Chicago：13 帧、27 区域、351 条区域-时间记录
- 系统定位：离线预测与可视化原型
- 非实时生产系统

**讲稿**

这里需要说明的是，前端不是直接加载完整 `.npy` 数据，而是读取后端导出的代表性 JSON 样本。这样做是为了保证浏览器交互性能。系统目前定位为离线预测与可视化原型，尚未接入实时交通接口，也不是生产级实时交通平台。后续可以扩展实时数据接入、真实 GIS 边界和在线更新能力。

## 第 12 页 项目部署与运行

**页面内容**

- GitHub 下载需要 Git LFS
- 启动方式：`python -m http.server 8000`
- 打开：`dashboard_fixed.html`
- 不能直接用 Download ZIP

**推荐图**

- `assets/paper-deployment-workflow.png`
- 或 Mermaid：`论文图表汇总.md` 图 9

**讲稿**

项目已经上传到 GitHub。由于数据集和模型权重较大，`.npy` 和 `.h5` 文件使用 Git LFS 管理。因此协作者下载时需要执行 `git lfs pull`，不能只使用 Download ZIP。前端运行时需要在项目根目录启动本地 Web 服务，然后通过浏览器访问 `dashboard_fixed.html`。

## 第 13 页 总结

**页面内容**

- 构建了双城交通碰撞风险预测数据流程
- 设计并训练了 Attention-LSTM 风险预测模型
- 完成多模型对比和消融实验
- 实现前端可视化大屏
- 支持风险热力图、真实标签校验和空间解释

**讲稿**

总体来说，本项目完成了从数据构建、模型训练、实验评估到前端可视化展示的完整流程。实验结果表明，Attention-LSTM 结合平滑和后处理机制能够较好地识别交通碰撞高风险样本。前端系统进一步将风险概率、标签校验和空间拓扑关系可视化，为模型结果解释和答辩展示提供了支撑。

## 第 14 页 不足与展望

**页面内容**

- 当前为离线原型系统
- 前端采用代表性时间帧抽样
- 尚未接入实时交通接口
- 后续可补充真实 GIS 边界、天气、施工和大型活动数据
- 可扩展在线预测和实时预警

**讲稿**

本项目仍有一些不足。首先，系统目前是离线原型，没有接入实时交通接口；其次，前端为了性能采用代表性时间帧展示，而不是全量时序展示；另外，区域语义和地图边界还可以进一步使用真实 GIS 数据增强。后续工作可以从实时数据接入、多源外部因素融合和在线预警系统部署三个方向继续改进。

## 时间分配建议

| 部分 | 页码 | 建议时间 |
| --- | --- | ---: |
| 背景与数据 | 1-4 | 2 分钟 |
| 模型方法 | 5-6 | 1.5 分钟 |
| 实验结果 | 7-9 | 2 分钟 |
| 系统展示 | 10-12 | 2 分钟 |
| 总结展望 | 13-14 | 1 分钟 |

## 答辩时最稳的核心表述

本系统使用 NYC 和 Chicago 交通时序数据、事故标签、空间邻接关系和模型评估结果完成训练与分析。前端为了保证交互性能，没有直接加载全量 `.npy` 文件，而是通过导出脚本抽取代表性时间帧，并生成前端可读 JSON 文件进行可视化展示。系统目前定位为离线预测与可视化原型，不是实时生产级交通平台。
