# 论文正文写作框架

本文档用于把项目内容整理成毕业论文正文。可以按学校模板调整章节名称。

## 摘要

本文围绕城市交通碰撞风险预警问题，设计并实现了一套基于 Attention-LSTM 的交通碰撞风险预测与可解释分析系统。系统以 NYC 和 Chicago 双城交通时序数据为基础，结合事故标签、区域坐标映射以及道路、POI 和历史记录等空间邻接关系，构建交通风险预测数据集。模型部分采用 Attention-LSTM 结构建模交通风险的时间依赖特征，并引入平滑门和流式后处理机制提升预测结果的稳定性。实验部分将本文模型与 GSNet、STG2Seq、ConvLSTM、LSTM、LightGBM、XGBoost、ARIMA、HA 等方法进行对比，并通过消融实验验证关键模块的有效性。系统实现部分进一步将模型预测结果、真实标签校验、实验指标和空间拓扑关系导出为前端可读 JSON，并构建可视化大屏展示风险热力图、TP/FP/FN 校验、模型指标和局部解释信息。实验结果表明，本文方法在双城数据集上具有较好的风险识别能力，能够为交通安全预警和可视化分析提供参考。

关键词：交通碰撞预测；Attention-LSTM；时序建模；风险预警；可解释分析；可视化系统

## 第 1 章 绪论

### 1.1 研究背景

可写要点：

- 城市交通系统规模扩大，交通碰撞与拥堵问题突出
- 交通安全管理需要提前识别高风险区域
- 交通风险具有明显的时间变化和空间关联
- 深度学习方法适合处理复杂时序数据

### 1.2 研究意义

可写要点：

- 理论意义：探索 Attention-LSTM 在交通风险预测中的应用
- 实践意义：辅助交通管理部门发现潜在高风险区域
- 系统意义：实现从模型结果到前端可视化解释的完整流程

### 1.3 本文主要工作

建议写成 4 点：

1. 构建 NYC 和 Chicago 双城交通风险预测数据流程。
2. 设计基于 Attention-LSTM 的交通碰撞风险预测模型。
3. 通过多模型对比和消融实验验证模型有效性。
4. 实现交通风险预测可视化大屏，展示风险热力图、标签校验和空间邻接解释。

### 1.4 论文组织结构

简述每一章内容。

## 第 2 章 相关技术与理论基础

### 2.1 交通风险预测任务

说明交通碰撞风险预测的输入、输出和评价目标。

### 2.2 时间序列建模

介绍 RNN、LSTM 对序列数据的处理能力。

### 2.3 Attention 机制

说明 Attention 用于突出关键时间片或关键特征。

### 2.4 模型评价指标

重点解释：

- AUC-PR
- AUC-ROC
- F1
- Accuracy
- Recall

强调交通碰撞数据类别不平衡，因此不能只看 Accuracy。

## 第 3 章 数据集构建与预处理

### 3.1 数据来源与结构

使用表格：

- `论文表格汇总.md` 表 1

### 3.2 时序数据构建

说明 `data_*.npy` 的结构：

```text
time × region × feature
```

### 3.3 标签数据构建

说明 `label.npy` 表示每个区域和时间片是否存在碰撞或异常风险。

### 3.4 空间邻接关系构建

说明：

- road adjacency
- poi adjacency
- record adjacency

### 3.5 前端展示样本导出

说明前端为什么只展示代表性帧：

- 浏览器不能直接加载大型 `.npy`
- 通过 JSON 保证交互性能
- 完整训练和评估仍在后端完成

可引用：

- `论文表格汇总.md` 表 2

## 第 4 章 模型设计

### 4.1 模型总体结构

可插入：

- `assets/paper-model-structure.png`
- `论文图表汇总.md` 图 4

### 4.2 输入特征表示

说明历史窗口、区域特征和空间邻接特征如何进入模型。

### 4.3 Attention-LSTM 风险预测模型

可写：

- Attention 模块为不同时间片或特征分配权重
- LSTM 捕捉交通风险随时间变化的趋势
- 输出层给出风险概率

### 4.4 平滑门与流式后处理

说明作用：

- 降低预测结果抖动
- 提高连续时间片的稳定性
- 更符合交通预警业务需求

## 第 5 章 实验设计与结果分析

### 5.1 实验设置

说明数据集、对比模型和指标。

### 5.2 多模型对比实验

插入：

- `论文表格汇总.md` 表 3、表 4
- `assets/paper-experiment-comparison.png`

分析重点：

- Myplan 在 AUC-PR、AUC-ROC、F1、Recall 上表现较好
- 对交通安全预警来说 Recall 更重要

### 5.3 消融实验

插入：

- `论文表格汇总.md` 表 5、表 6
- `assets/paper-ablation-study.png`

分析重点：

- 去掉平滑门或流式后处理后性能下降
- 完整模型具有更稳定的综合表现

### 5.4 实验结果总结

总结模型有效性和不足。

## 第 6 章 系统设计与实现

### 6.1 系统总体架构

插入：

- `论文图表汇总.md` 图 1

### 6.2 后端训练与导出模块

说明：

- `train.py`
- `infer_and_export_frontend.py`
- `export_frontend_predictions.py`
- `export_frontend_metrics.py`
- `export_frontend_topology.py`

### 6.3 前端可视化模块

插入：

- `assets/frontend-real-screenshot.png`

说明功能：

- 城市切换
- 时间轴播放
- 风险热力图
- TP/FP/FN 校验
- 模型指标图
- 局部邻接解释

### 6.4 系统部署与运行

插入：

- `assets/paper-deployment-workflow.png`

说明 Git LFS 和本地 Web 服务。

## 第 7 章 总结与展望

### 7.1 工作总结

总结：

- 完成数据构建
- 完成模型训练和评估
- 完成消融实验
- 完成前端可视化系统

### 7.2 不足

可以写：

- 当前系统为离线原型
- 前端展示为代表性时间帧
- 尚未接入实时交通数据
- 地图边界和外部事件数据仍可增强

### 7.3 展望

可以写：

- 接入实时交通流和事故接口
- 融合天气、施工、节假日、大型活动数据
- 使用真实 GIS 边界增强地图展示
- 部署在线风险预警服务

